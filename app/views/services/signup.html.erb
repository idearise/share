<% content_for :page_title do -%>New Account<% end -%>
<% content_for :content_header do %>
<div class="page-header">
  <h1>New Account</h1>
</div>	
<% end %>

<div class="span8 pull-left">
	<h2>This <%= @authhash[:provider].capitalize %> login is new to this site</h2>
	<h5>Service: <%= @authhash[:provider].capitalize %></h5>
			<!-- <h5>ID: <%#= @authhash[:uid] %></h5> -->
	<h5>Name: <%= @authhash[:name] %></h5>
	<h5>Email: <%= @authhash[:email] %></h5>

	<%= form_tag :controller => "services", :action => "newaccount", :class => "nice" do -%>
		<%= recaptcha_tags(:ssl => true) if !Rails.env.development? -%>
		<div class="add-top">
			<button type="submit" class="btn primary">Create A New Account</button> &nbsp;<%= submit_tag "Cancel", :class=>"btn" -%>
		</div>
	<% end -%>
</div>

<div class="span7 pull-right">
	<h3>Already have an account?</h3>
	<p>
		<strong>If you think you already have an account, and you were just trying to associate a new login service with your existing account, you’re in the wrong place. You’re not logged into the site right now.</strong>
	</p>
	<p>
		If you already have an account, and you’d like to associate another login service with it, here’s what to do:
	</p>
	<ol>
		<li>Login to <strong><%= t(:site_name) -%></strong> with your existing account's login service (example: your Google account)</li>
		<li>Go to your profile page (link is located at the top+right corner of the page)
		<li>Add one or more services to be used to sign-in (example: your Yahoo! account)</li>
	</ol>
</div>
