<script id="_post" type="text/x-handlebars-template">
  <div class="add-bottom">
  <div class="pull-left"><img src="{{picture_small}}" class="inline"></div>
  <div class="post-summary">
    <h4><a href="/apps/{{dimension_keys}}/posts/{{id}}">{{title}}</a> <small>at {{domain}} +{{up}} -{{down}}</small> <span class="label source"><a href="{{url}}">link</a></span></h4>
    <span class="nowrap">{{username}}</span> 
    &bull; {{created_at}}  
    &bull; {{dimension_names}}
    &bull; {{comments}} responses
  </div>
</div>
</script>

